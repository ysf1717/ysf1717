public class Ternary {

    public static void main(String[] args) {
        
        int n =5;
        int result = 0;

        result = n%2==0 ? 10 : 20;
        // ? resembles if passes print 10 . If even
        // : resembles else print 20. ODD
        System.out.println(result);                                      
    }
    
}
